name: bitcoin_ticker
description: A new Flutter application.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^3.0.0
  cupertino_icons: ^0.1.2
  javango_lib:
    path: ../javango_lib

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
